# 🔥学習記録🔥
## 2/26(木)  2.5時間
プログラミング学習(2.0時間) 作業場所1:鶴橋カフェ　コンセントあるがいいが、テーブル小さいのがネック。
												作業場所2:自宅
削除機能について、削除ボタンを押しても削除されない状態のエラーをAIに聞きながら解決。
深掘りとまとめを明日行う。

- CCNA取得について
0.5時間
昼やすみに動画を見て、家に帰ってパケットトレーサーで実装。
ルーティングについて、ホストのPCにデフォルトゲートウェーイを登録していないことで、
pingが通らなかった。
なぜデフォルトゲートウェーイを登録する必要があるのか調べる。

- 仕事
  障害対応についてログを調べながら進めた。ポモロードテクニックを使いながら作業を分けると効果的。
	どうしても調べることが多くなるので、ダラダラしまいがちだったがメリハリがつく感じがした。
  調べながら作業を進める。社内の出荷判定に向けた動きを行っている。

- 就活 0時間
 - 全くしていない。まずいな。明日企業選定30社行う。

エラー内容
```
application-705d6943d511709b4a5b04aa4cffe14fc73cf5bbc9a8ba70c90bf81e0f4f9db3.js:9045 Racket Id: 1
application-705d6943d511709b4a5b04aa4cffe14fc73cf5bbc9a8ba70c90bf81e0f4f9db3.js:9046 Racket Element: div
application-705d6943d511709b4a5b04aa4cffe14fc73cf5bbc9a8ba70c90bf81e0f4f9db3.js:9047 Current User Id: 3
2content.js:1 Uncaught (in promise) The message port closed before a response was received.
(anonymous) @ content.js:1Understand this error
application-705d6943d511709b4a5b04aa4cffe14fc73cf5bbc9a8ba70c90bf81e0f4f9db3.js:9054 Connected to comments channel for racket
1:1 <meta name="apple-mobile-web-app-capable" content="yes"> is deprecated. Please include <meta name="mobile-web-app-capable" content="yes">
```
```
if(data.action === 'create'){
    const tableComment = document.getElementById('table-comment');

    if(tableComment){
      tableComment.insertAdjacentHTML('afterbegin',data.comment);
      const newComment = tableComment.firstElementChild;
      const commentUserId = newComment?.dataset.userId;

      console.log('Comment User Id:', commentUserId);
      console.log('Current User Id:', currentUserId);

      if(commentUserId !== currentUserId) {
        const deleteButton = newComment.querySelector('.delete-button');
        if (deleteButton){
          deleteButton.style.display = 'none';
          console.log('Delete button hidden for other user ');
        }
      }
    } else {  // ← ここに else を移動
      console.error("Comments container not found");
    }
  }
	```
	```
	    if(data.action === 'create'){
    // コメントIDで条件分岐をする際に変数を設定をした方が少なくて済むため変数を追加
    const tableComment = document.getElementById('table-comment');

    if(tableComment){
      //HTMLを挿入
      tableComment.insertAdjacentHTML('afterbegin',data.comment);

      //挿入した最新のコメント要素を取得
      const newComment = tableComment.firstElementChild; //タイポを修正 `firstELementChild`からfirstElementChild`

     //コメント投稿者のIDを取得
     const commentUserId = newComment?.dataset.userId;

     console.log('Comment User Id:', commentUserId);
     console.log('Current User Id:', currentUserId);

      //投稿者と現在のユーザーが異なる場合、削除ボタンを非表示
      if(commentUserId !== currentUserId) {
        const deleteButton = newComment.querySelector('.delete-button');
        if (deleteButton){
          deleteButton.style.display = 'none';
          console.log('Delete button hidden for other user '); //削除ボタンが投稿ユーザー以外で消えているかの確認のためにボタン
        }
      }
    } else {
      console.error("Comments container not found or no comment data");
     }
    }