# 🔥学習記録🔥
## 4/4(金) 1.0時間
作業場所1:移動しながら
- プログラミング学習(1時間)
  - profileに有無により、名前を表示するように変更
	`syntax error`が発生。エラーログを読んでいても`end`の位置や内容については間違っていないようだった。
	 ```
	  # エラー箇所
	  <%= if racket.user.profile.present? %>

   # 修正
	  <% if racket.user.profile.present? >
	```
	エラーが出た際にもう少し細かく読むようにする。エラーの意味を理解していない気がした。
	`<%= >`の意味がわかっていない。。。

	- 名前を押すとプロフィール詳細画面に遷移するように変更。
     発生エラー
		 ボタンを押すと投稿したuser_idをprofileコントローラーのshowアクションに送っていた。
		 ```
		  # 変更前
     <%= link_to racket.user.name, profile_path(racket.user) %>
      # 変更後
      <%= link_to racket.user.name, profile_path(racket.user.profile) %>
		 ```

- CCNA取得について(0時間)
休日

- 仕事
休日

- 就活


- その他
自分がこれからどうするのか？
インフラ、開発問わず、若い企業伝探してみるのもありかも。
→自分がそのサービスに熱意を持てるか。
できる受託でもいいので、運用・保守まで一貫してサービスに携わりたい。
人があまりやりたがらないけど必要な仕事に目を向けるのも一つ。

スーツを着ているのはなんか怪しい。
ラフな感じの方が浸し身が持てる。


